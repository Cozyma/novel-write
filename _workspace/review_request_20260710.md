# レビュー依頼（2026-07-10・検査セッション用パケット）

このファイルは別エージェント（レビュアー）へそのまま渡す依頼文。レビュアーは**検査モード**で動く：**指摘のみ・生成や書き直しをしない・依頼に列挙されたファイル以外を読まない**。各依頼は独立──片方だけ実施してもよい（1エージェント1依頼を推奨＝文脈の相互汚染を避ける）。

---

## 依頼1: 運用導入の監査（WORK_MASTER方式）

**役割**: 本日導入したセッションモード分離＋作業マスタ方式の設計監査。導入目的＝「目標ドリフトの早期可聴化」（発生源の診断＝生成主体の最適化圧＋自己認証・検査主体の分離が対策）。

**読むもの（これ以外は読まない）**:
1. `_workspace/WORK_MASTER.md`（全文）
2. `00_system/ARCHITECTURE.md` の「3. イベント・運用フック」節のみ
3. `_workspace/TODO.md` の冒頭（§0ポインタ）のみ

**設問**:
- Q1 単一書き手の穴: 作話・検査セッションがマスタ本体を書き換えてしまう抜け道（曖昧な文言・未定義の状況）はどこか。
- Q2 マスタ自身のドリフト経路: §2目標スタック・§3トラック表が実態と乖離していく最もありそうな筋を挙げよ（更新責務が管理セッションに集中することの副作用を含む）。
- Q3 モード定義の曖昧箇所: 「作話／検査／管理」の境界で判断に迷う具体的状況を挙げよ（例: レンダー中に発見した整合バグの修正はどこまで作話か）。
- Q4 発効手順の矛盾: 「改定の発効は次セッションから」と各フック・鉄則の間に矛盾や実行不能な点はないか。

**出力形式**: 指摘リスト（番号・重大度 高/中/低・該当箇所の引用・一行の根拠）。改善案は求めない（指摘だけでよい。書きたければ各指摘に1行まで）。

---

## 依頼2: 話3改v1の目標適合チェック（コールド）

**役割**: 場面が目標に「効いているか」の敵対的検査。設計経緯・自己正当化を知らない目で、**画面に書かれていることだけ**から判定する。

**読むもの（これ以外は読まない。特に decision_log・判定ログ・CONTEXT は読まない）**:
1. `_workspace/probes/renders/ep3_rework_v1.md` の「## 話3改」セクション（本文のみ。同ファイル内の decision_log 以下は**読まない**）
2. 前提となる目標（下に転記済み・他ファイル参照不要）

**目標（転記）**:
- 子目標G2: 話1の夜（女が殺されかけた——誰が・なぜ・まだ危ないのか）の重力を、この話が引き継いで**前進**させること。
- 場面の存在理由: 前進・緊迫・報酬のうち少なくとも一つを**画面内で**成立させること（設置・説明だけの場面は不可）。
- 前提知識（本文理解に必要な最小限）: 前話までに——夜、主人公は路地で見張り2人を眠らせ、女（店主スィグナ）を暗殺者から救った。翌朝、役人の検分で店主が「うちの者です」と嘘の名義引受をして彼を庇い、割符（店の名義で働く札）を渡した。「細かい話は板の前で」「危ない札は取らない」という約束がある。

**設問**:
- Q1 この場面で**画面内に強制された変化**（人物の状態・関係・選択・制約が場面の前後で変わったもの）を列挙せよ。無ければ「無い」と書け。
- Q2 次の主張それぞれについて、**本文に実装されている**か**宣言・雰囲気止まり**かを分類し根拠の行を引用せよ: (a)夜の重力の前進 (b)スィグナ同伴の意味 (c)見張りの男の出現の意味 (d)「危ない札は取らない」の約束が機能した (e)〆の不穏が次話を読む理由になる。
- Q3 初見の読者として、意味が取れない・存在理由が分からない要素を挙げよ。
- Q4 この話を丸ごと削って話4に繋いだ場合、読者が失うものを一行で言え（言えなければそれ自体が所見）。

**出力形式**: Q1〜Q4に番号順で回答。判定語は「実装/宣言のみ/不明」の三値。面白さの評価はしない（それは作者オラクルの領分）。

---

## 依頼の出し方（親セッション向けメモ）
- 新しいセッション（またはサブエージェント）に本ファイルの該当依頼**だけ**を渡す。依頼1と依頼2は別エージェントで（相互汚染防止）。
- 結果の記帳は親が行う: 受領→ WORK_MASTER §5受信箱へ1行＋詳細はCONTEXTへ。レビュアーにマスタを書かせない。
- 注意: 話3改はv2再配線案が人間判定待ち。依頼2をv1に対して打つ場合は「v2設計の裏取り」として使い、v2確定後は本依頼のファイル指定をv2に差し替えて再利用可。
