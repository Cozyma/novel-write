# レビュー依頼の親用メモ（2026-07-10・改訂＝指摘4件反映）

依頼文の正本は `_workspace/review_requests/` 配下＝**1依頼1ファイル**（レビュアーには該当ファイルだけを渡す。本メモは渡さない）:
- 依頼1（運用導入の監査）: `review_requests/req1_ops_audit_20260710.md`
- 依頼2（話3v1の目標適合チェック・コールド）: `review_requests/req2_ep3v1_goal_check_20260710.md` ＋ 対象本文の抽出版 `req2_target_ep3v1_body.md`（decision_log等を含まない）

## 出し方
- 依頼1と依頼2は**別エージェント**で（相互汚染防止）。
- レビュアーはセッション開始フックの適用外（ARCHITECTURE [Hook: セッション開始時] の検査パケット例外・2026-07-10追記）＝パケット記載ファイルのみ読む。
- 結果の記帳は親が行う: 受領→WORK_MASTER §5受信箱へ1行＋詳細はCONTEXTへ。レビュアーにマスタを書かせない。

## 反映済みの指摘（2026-07-10レビュー）
- 高: フックが全セッションにCONTEXT等を読ませる→コールド不成立 → **検査パケット例外をフックに新設**＋依頼2の対象を本文抽出版に変更。
- 中: 「そのまま渡す」と「該当依頼だけ渡す」の矛盾 → 1依頼1ファイルへ分割・本メモは親専用。
- 中: 依頼2のQ4が参照範囲外（話4）を要求 → 「この本文でしか起きていない変化のうち失われるもの」へ改稿。
- 低: 部分実装の扱い未定義 → 「部分実装は宣言のみへ分類」を明記。

## 状態メモ（親のみ）
話3改はv2再配線案が人間判定待ち。依頼2のv1向け結果は「v2設計の裏取り」として使い、v2確定後は req2 の対象本文を差し替えて再利用可。
