# 累積カウンタ台帳——本文から再構成困難な「全話横断の合計値」のみを置く。
# 正本は本文。更新は章完成・人間判定通過後（README §3 / ARCHITECTURE.md 2.1「本文と動的状態」参照）。
# 現在地・負傷・疲労などの短距離状態はここで管理しない（直近の本文・章サマリーから再構成する）。

characters:
  - id: nork
    name_ja: "ノルク・ヴァルグ"
    # 目撃露出度（2026-07-05再定義＝旧・不協和音の痕跡蓄積〔波形監視〕を機構ごと廃止）。
    # 「説明のつかない力」を人前で見せた度合いの累積＝噂・神話化の燃料。誤読（謎の加護持ち）が効く限り露出は無害化されるが、
    # 蓄積は『何か本物の異常がいる』の確信を社会に育てる（concept.yaml / misunderstanding_escalation）。
    # 第1アークは段0〜1と受動観測のみ＝人目に晒る異常が小さい。加算基準は機械判定せず人間が判断する。
    # 閾値による追跡イベントの自動発火は行わず、投入タイミングはストーリー優先で人間が判断する（2026-06-10合意）。
    # 定義は 01_static_database/world/setting_magic.yaml / concealment_rule を参照。
    exposure_level: 0
